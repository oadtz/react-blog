import React from 'react';
import expect from 'expect';

import PostCreate from 'PostCreate';

describe('PostCreate', () => {
  it('should exist', () => {
    expect(PostCreate).toExist();
  });

});
