import React from 'react';
import expect from 'expect';

import HtmlContent from 'HtmlContent';

describe('HtmlContent', () => {
  it('should exist', () => {
    expect(HtmlContent).toExist();
  });

});
