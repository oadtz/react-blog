import React from 'react';
import expect from 'expect';

import PostActions from 'PostActions';

describe('PostActions', () => {
  it('should exist', () => {
    expect(PostActions).toExist();
  });

});
